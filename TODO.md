# TODO

---

## Тестирование

- [ ] **Добавить unit-тесты для сервисов**  
      — `AuthService`, `UsersService`, `PostsService`,
      `NotificationsService`, `FriendshipsService`, `FollowService`,
      `BlockService`  
      — Использовать моки для Prisma и внешних зависимостей.

- [ ] **Расширить e2e-тесты**  
      — Покрыть сценарии с OAuth2 (Google/GitHub) — потребуется
      мок-сервер.  
      — Проверить кейсы с блокировками (удаление подписок/дружбы).  
      — Проверить массовые операции с уведомлениями.

- [ ] **Настроить coverage-пороги**  
      — Минимум 80% покрытия для критических модулей.

- [ ] **Добавить нагрузочное тестирование**  
      — Для эндпоинтов с большим трафиком (посты, уведомления).

---

## Документация

- [ ] **Дополнить README.md**  
      — Инструкция по локальной разработке (с переменными
      окружения).  
      — Схема БД (ER-диаграмма).  
      — Примеры запросов (curl/postman).

- [ ] **Написать руководство для контрибьюторов**  
      — Code style (Prettier, ESLint).  
      — Как добавить новый модуль (структура, регистрация).

- [ ] **Актуализировать Swagger-аннотации**  
      — Добавить описания для всех DTO и ответов ошибок.  
      — Указать примеры для query-параметров.

- [ ] **Создать документацию по миграциям**  
      — Как создавать и применять миграции Prisma.

---

## Безопасность

- [ ] **Усилить CORS-политику**  
      — Использовать динамический список разрешённых origin (не только
      `CLIENT_URL`).

- [ ] **Добавить валидацию на всех входных данных**  
      — Проверить, что `class-validator` применён ко всем DTO.

- [ ] **Настроить безопасные cookie для сессии**  
      — `secure: true` в production, `sameSite: 'lax'` или `'strict'`.

- [ ] **Реализовать refresh-токены**  
      — Текущий JWT живёт долго (`28d`), стоит добавить механизм
      обновления.

- [ ] **Ограничить количество попыток входа**  
      — Защита от брутфорса (можно через Throttler или отдельный
      модуль).

---

## Производительность

- [ ] **Оптимизировать запросы Prisma**  
      — Использовать `select` вместо `include` где возможно.  
      — Добавить составные индексы для частых фильтров (например,
      `(recipientId, readed, createdAt)` уже есть, но проверить все).

- [ ] **Внедрить кэширование запросов**  
      — GET /posts (список) — кэшировать на 5–10 секунд.  
      — GET /users/:slug — кэшировать на 30 секунд.

- [+] **Переключиться на пагинацию с курсором**  
  — Вместо `offset/limit` для больших таблиц (посты, уведомления).

- [ ] **Настроить пул соединений с БД**  
      — Использовать `connectionLimit` в Prisma.

---

## Функциональность

- [ ] **Добавить комментарии к постам**  
      — Модель `Comment` уже есть, но нет контроллеров и сервисов.

- [ ] **Реализовать реакции (лайки) к постам и комментариям**  
      — Модели `PostReaction` и `CommentReaction` есть, но API
      отсутствует.

- [ ] **Поиск по пользователям и постам**  
      — Полнотекстовый поиск в PostgreSQL (с использованием
      `tsvector`).

- [ ] **Загрузка аватаров / изображений**  
      — Интеграция с облачным хранилищем (S3, Cloudinary).

- [ ] **Уведомления в реальном времени**  
      — Через WebSockets (Socket.io) для мгновенного обновления.

---

## Рефакторинг

- [ ] **Унифицировать обработку ошибок**  
      — Все ошибки должны проходить через
      `ExceptionHandlerRegistry`.  
      — Добавить больше специфичных обработчиков (например, для
      валидации).

- [ ] **Улучшить `CrudService`**  
      — Вынести общие методы в абстрактный класс с дженериками.  
      — Поддержка `soft delete`.

- [ ] **Перейти на `ES modules`**  
      — Сейчас `"type": "commonjs"`, но современный NestJS
      поддерживает ESM.

---

## Инфраструктура

- [ ] **Оптимизировать Dockerfile**  
      — Использовать multi-stage сборку эффективнее (уже есть, но
      можно улучшить кэширование).  
      — Добавить `healthcheck` для контейнера `app`.

- [ ] **Настроить CI/CD для прогона тестов**  
      — В GitHub Actions добавить шаг `pnpm test` перед деплоем.

- [ ] **Добавить поддержку разных окружений**  
      — `.env.development`, `.env.production`, `.env.test` — уже
      частично реализовано, но проверить.

- [ ] **Развернуть демо-версию**  
      — На бесплатном хостинге (Render, Heroku) для презентации.

---

## Технический долг

- [ ] **Настроить pre-commit хуки**  
      — Использовать `husky` + `lint-staged` для автоформатирования.
